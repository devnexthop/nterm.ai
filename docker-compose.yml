services:
  relay:
    build: .
    ports:
      - "8787:8787"
      - "514:514/udp"
      - "69:69/udp"
      - "67:67/udp"
    environment:
      RELAY_DATA_DIR: /data
    volumes:
      - ./data:/data
    cap_add:
      - NET_BIND_SERVICE
      - NET_ADMIN
    restart: unless-stopped
