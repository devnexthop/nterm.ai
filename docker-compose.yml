services:
  nterm:
    build: .
    image: nterm:local
    ports:
      - "8787:8787"
    environment:
      NTERM_DATA_DIR: /data
      NTERM_BENCH_URL: https://nterm.ai/bench-feed.json
    volumes:
      - ./data:/data
    restart: unless-stopped
